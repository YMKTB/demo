package com.ymktb.conferencedemo.repositories;

import com.ymktb.conferencedemo.models.Anno;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AnnoRepository extends JpaRepository<Anno,Long> {
}
